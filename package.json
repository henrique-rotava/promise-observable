{
  "name": "promise-observable",
  "version": "1.0.31",
  "description": "A fusion between Promises ans Observables",
  "scripts": {
    "build": "npx tsc",
    "pub": "yarn build && cp package.json dist && cp README.md dist && cd dist && npm publish",
    "test": "jest"
  },
  "files": [
    "dist/*.js",
    "dist/**/*.js",
    "dist/**/*.ts",
    "dist/**/*.map"
  ],
  "author": "Henrique Rotava",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/henrique-rotava/promisable"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/jest": "^27.5.1",
    "jest": "^28.1.0",
    "ts-jest": "^28.0.3",
    "ts-node": "^10.8.0",
    "typescript": "^4.7.2"
  }
}
